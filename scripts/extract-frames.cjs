const fs = require('fs'); const path = require('path'); const { execSync } = require('child_process'); const ffmpegPath = require('ffmpeg-static'); const videoPath = path.join(__dirname, '../public', 'hero-repair-animation.mp4'); const framesDir = path.join(__dirname, '../public', 'frames'); if (!fs.existsSync(framesDir)) fs.mkdirSync(framesDir); const cmd = '\"' + ffmpegPath + '\" -i \"' + videoPath + '\" -r 30 -qscale:v 4 \"' + path.join(framesDir, 'frame_%03d.jpg') + '\"'; console.log('Running:', cmd); execSync(cmd, { stdio: 'inherit' });
