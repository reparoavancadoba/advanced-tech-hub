const { allPosts } = require('./src/data/blogData.ts'); 
// Wait, I can't require TS directly like this unless I use tsx or transpile it, 
// I will write a simple script that reads the transpiled/ts-node output, or I'll just write it in TS and run with npx tsx.
